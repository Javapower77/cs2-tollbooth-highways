using Unity.Entities;

namespace TollboothHighways.Domain.Components
{
    // Temporary marker to exclude a lane for the active vehicle category path batch.
    public struct TollLaneTempBlocked : IComponentData {}
}